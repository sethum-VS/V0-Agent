# Copy to .env.local and run: vercel link && vercel env pull .env.local --yes
# AI Gateway auth (OIDC is preferred on Vercel; pull populates this locally):
# VERCEL_OIDC_TOKEN
# Optional static key for CI or non-Vercel hosts:
# AI_GATEWAY_API_KEY
